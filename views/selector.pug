extends layout

block content
  h1= title
  p I AM IRONMAN 
   - var image_names=[ "range rover.jpg","mustang.jpg","ram.jpg","classic mustang.jpg","offroad ram.jpg"]
    -for(let i=0; i<4; i++){
      -let random= Math.floor(Math.random()*5)
       img(src=`/images/${image_names[random]}` alt=`Image No ${random}` height="300px" width="300px")
      - }
